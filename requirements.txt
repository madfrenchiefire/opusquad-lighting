# Core
pyyaml>=6.0
fastapi>=0.111
uvicorn[standard]>=0.29

# Audio analysis (XR-18 / ASIO)
# pip install sounddevice numpy scipy
# ASIO support on Windows: set SD_ENABLE_ASIO=1 (handled automatically)
sounddevice>=0.4
numpy>=1.24
scipy>=1.11

# MIDI controller input
# pip install mido python-rtmidi
mido>=1.3
python-rtmidi>=1.5

# OSC server (TouchOSC / Lemur)
# pip install python-osc
python-osc>=1.8
